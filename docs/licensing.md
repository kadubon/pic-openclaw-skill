# Licensing

The repository root remains Apache-2.0.

The Python helper CLI, docs, tests, schemas, examples, root `SKILL.md`, and `skill/pic-residual-guard/` copy remain Apache-2.0 unless a file states otherwise.

The ClawHub submission bundle under `clawhub/pic-residual-guard/` is MIT-0. That bundle has a bundle-local `LICENSE` file with `SPDX-License-Identifier: MIT-0`.

This license split is intentional. Do not silently change the whole repository license to satisfy a registry workflow.

For manual OpenClaw installation from this repository, use the repository `LICENSE` and the skill directory `LICENSE`.

For ClawHub submission, submit `clawhub/pic-residual-guard/`, not the repository root.
