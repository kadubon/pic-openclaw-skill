# Licensing

The repository code, docs, schemas, examples, helper CLI, and skill text are Apache-2.0 unless a file states otherwise.

If a skill registry requires a different license for submitted skill text, publish only after an explicit license decision. Do not silently change the whole repository license to satisfy a registry workflow.

For manual OpenClaw installation from this repository, use the repository `LICENSE` and the skill directory `LICENSE`.
